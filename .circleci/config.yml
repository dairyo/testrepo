version: 2
jobs:
   build:
     docker:
      - image: docker:stable-git
     steps:
       - checkout
       - setup_remote_docker
       - run:
           command: |
            pwd
            ls ./
            cd redash
            docker build -t .
       
           
